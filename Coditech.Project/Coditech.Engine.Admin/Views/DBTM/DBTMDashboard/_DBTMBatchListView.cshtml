@model GeneralBatchListViewModel
@{
	Layout = "~/Views/Shared/_LayoutCustomMobile.cshtml";
	ViewBag.Title = "All Batch Details List";
}
<input type="hidden" id="SelectedParameter1" value="@ViewBag.SelectedParameter1" />
<input type="hidden" id="SelectedParameter2" value="@ViewBag.SelectedParameter2" />
<div class="container-fluid mt-3">
    <div class="row">
        <div class="col-12">
            <div id="DataTablesDivId">
                @Html.Partial("~/Views/DBTM/DBTMDashboard/_DashboardBatchList.cshtml", Model)
            </div>

        </div>
    </div>
</div>
<script>
    function resizeParentIframe() {
        if (window.parent && window.parent !== window && window.frameElement) {
            window.frameElement.style.height =
                document.documentElement.scrollHeight + "px";
        }
    }
    $(document).ready(function () {
        resizeParentIframe();
        setTimeout(resizeParentIframe, 300);
        setTimeout(resizeParentIframe, 800);
    });
    window.addEventListener("load", resizeParentIframe);
</script>