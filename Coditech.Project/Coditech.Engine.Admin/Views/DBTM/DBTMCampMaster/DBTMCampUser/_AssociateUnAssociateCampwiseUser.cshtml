@using Coditech.Admin.Utilities;
@model DBTMCampUserViewModel
@{
    Layout = null;
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
}
<!-- Page Content-->
<div class="modal-header">
    @if (Model.DBTMCampUserId > 0)
    {
        <h6 class="modal-title m-0" id="exampleModalDefaultLabel">UnAssociate Camp From User</h6>
    }
    else
    {
        <h6 class="modal-title m-0" id="exampleModalDefaultLabel">Associate Camp To User</h6>
    }
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div><!--end modal-header-->
@using (Html.BeginForm("AssociateUnAssociateCampwiseUser", controllerName, FormMethod.Post, new { id = "frmAssociateUnAssociateCampwiseUser" }))
{
    @Html.AntiForgeryToken();
    @Html.HiddenFor(x => x.DBTMCampUserId)
    @Html.HiddenFor(x => x.DBTMCampMasterId)
    @Html.HiddenFor(x => x.DBTMTraineeDetailId)
    <div class="modal-body">
        <div class="row">
            <div class="col-lg-12">
                <div class="mb-3">
                    @if (Model.DBTMCampUserId > 0)
                    {
                        <span>
                            Are you sure that you want to unassociate <span class="font-15"> @Model.CampName</span> Camp from <span class="font-15">@Model.FirstName @Model.LastName</span>?
                        </span>
                    }
                    else
                    {
                        <span>
                            Are you sure that you want to associate <span class="font-15">
                                @Model.CampName Camp
                            </span> to <span class="font-15">
                                @Model.FirstName @Model.LastName 
                            </span>?
                        </span>
                    }
                </div>
            </div>
        </div><!--end row-->
    </div>

    <!--end modal-body-->
    <div class="modal-footer">
        <button type="submit" id="saveDraft" class="btn btn-success" data-test-selector="linkSave" onclick="DBTMCampMaster.AssociateUnAssociateCampwiseUser(); return false;">@AdminResources.LabelSave</button>
        <button type="button" class="btn btn-soft-secondary btn-sm" data-bs-dismiss="modal">Close</button>
    </div>
}
@Html.Partial("_ValidationScriptsPartial")
