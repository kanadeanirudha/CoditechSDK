@model DBTMTraineeAssignmentViewModel
@{
    if (!string.IsNullOrEmpty(Model.Custom4))
    {
        Layout = "~/Views/Shared/_LayoutCustomMobile.cshtml";
    }
    ViewBag.Title = "Update Trainee Assignment";
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
    string actionName = ViewContext.RouteData.Values["action"].ToString();
}
<!-- Page Content-->
<div class="page-content mt-5">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <div class="row align-items-center">
                            <div class="col">
                                <h4 class="card-title">Update Trainee Assignment - @Model.FirstName @Model.LastName - @Model.TestName </h4>
                            </div><!--end col-->
                        </div> <!--end row-->
                    </div>
                    <div class="card-body">
                        @using (Html.BeginForm("GetDBTMTraineeAssignment", controllerName, FormMethod.Post))
                        {
                            @Html.HiddenFor(m => m.DBTMTraineeAssignmentId)
                            @Html.HiddenFor(m => m.GeneralTrainerMasterId)
                            @Html.HiddenFor(x => x.SelectedCentreCode)
                            @Html.HiddenFor(x => x.DBTMTraineeAssignmentUserId)
                            @Html.HiddenFor(x => x.Custom4)
                            <div class="row">
                                <div class="col-lg-4">
                                    <div class="mb-3">
                                        @Html.LabelFor(x => x.AssignmentDate, new { @class = "form-label required" })
                                        @Html.TextBoxFor(x => x.AssignmentDate, AdminGeneralHelper.DateFormatForTextBox(), new { @class = "form-control", @readonly = "true" })
                                        @Html.ValidationMessageFor(x => x.AssignmentDate)
                                    </div>
                                </div>
                                <div class="col-lg-4">
                                    <div class="mb-3">
                                        @Html.LabelFor(x => x.AssignmentTime, new { @class = "form-label" })
                                        @Html.TextBoxFor(x => x.AssignmentTime, new { @class = "form-control", type = "time" })
                                        @Html.ValidationMessageFor(x => x.AssignmentTime)
                                    </div>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-success">@AdminResources.LabelSave</button>
                            if (!string.IsNullOrEmpty(Model.Custom4))
                            {
                                <a href="../@controllerName/Cancel?SelectedCentreCode=@Model.SelectedCentreCode&GeneralTrainerMasterId=@Model.GeneralTrainerMasterId&SelectedParameter1=@Model.Custom4" class="btn btn-secondary"><i class="ft-x"></i>@AdminResources.LabelCancel</a>
                            }
                            else
                            {
                                <a href="../@controllerName/Cancel?SelectedCentreCode=@Model.SelectedCentreCode&GeneralTrainerMasterId=@Model.GeneralTrainerMasterId" class="btn btn-secondary"><i class="ft-x"></i>@AdminResources.LabelCancel</a>
                            }
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="~/js/coditech/DBTM/DBTMTraineeAssignment.js"></script>

@section scripts {
    <script type="text/javascript">
        $(function () {
            if (@Model.DBTMTraineeAssignmentId == 0) {
                $('#AssignmentDate').val('');
            }
            var today = new Date();
            var oneYearFromNow = new Date();
            oneYearFromNow.setFullYear(today.getFullYear() + 1);

            $("#AssignmentDate").datepicker({
                changeMonth: true,
                changeYear: true,
                dateFormat: "@AdminGeneralHelper.DateFormatForCalendar()",
                language: "en",
                autoSize: true,
                gotoCurrent: true,
                clearBtn: true,
                showButtonPanel: true,
                closeText: 'Clear',
                onClose: function (dateText, inst) {
                    if ($(window.event.srcElement).hasClass('ui-datepicker-close')) {
                        document.getElementById(this.id).value = '';
                    }
                },
                minDate: today,
                maxDate: oneYearFromNow
            });
        });
    </script>
}