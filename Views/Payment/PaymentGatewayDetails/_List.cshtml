@model PaymentGatewayDetailsListViewModel
@{
    Layout = null;
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
    string actionName = ViewContext.RouteData.Values["action"].ToString();
}
@* <div class="row">
    @Html.Partial("~/Views/Shared/Datatable/_DatatableShowResult.cshtml", Model.PageListViewModel.RecordPerPage)
    @Html.Partial("~/Views/Shared/Datatable/_DatatableSearchData.cshtml", Model.PageListViewModel.SearchBy)
</div> *@
<div class="row">
    <div class="col-sm-12 table-responsive">
        <table id="datatable" class="table table-striped table-bordered dt-responsive nowrap dataTable no-footer dtr-inline" style="border-collapse: collapse; border-spacing: 0px; width: 100%;" role="grid" aria-describedby="datatable_info">
            @Html.Partial("~/Views/Shared/Datatable/_DatatableColumns.cshtml", Model.PageListViewModel)
            <tbody>
                @if (Model?.PaymentGatewayDetailsList?.Count > 0)
                {
                    foreach (var item in Model?.PaymentGatewayDetailsList)
                    {
                        <tr>
                            <td>@item.Mode</td>
                            <td class="text-center data-action">
                                @if (item.PaymentGatewayDetailId > 0)
                                {
                                    <a href="../@controllerName/Edit?paymentGatewayDetailId=@item.PaymentGatewayDetailId" title="Update" class="btn btn-sm btn-soft-success btn-circle me-2">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                }
                                else
                                {
                                    <a href="../@controllerName/Create?centreCode=@Model.SelectedCentreCode&paymentGatewayId=@Model.SelectedParameter1&isLiveMode=@item.IsLiveMode&paymentCode=@item.PaymentCode" title="Create" class="btn btn-sm btn-soft-success btn-circle me-2">
                                        <i class="fas fa-plus"></i>
                                    </a>
                                }
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
