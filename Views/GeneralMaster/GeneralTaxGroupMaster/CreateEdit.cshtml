@model GeneralTaxGroupMasterViewModel
@{
    ViewBag.Title = Model.GeneralTaxGroupMasterId > 0 ? "Update Tax Group Master" : "Add Tax Group Master";
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
    string actionName = ViewContext.RouteData.Values["action"].ToString();
}
<!-- Page Content-->
<div class="page-content mt-5">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <div class="row align-items-center">               
                        <div class="col">
                            @if (Model.GeneralTaxGroupMasterId > 0)
                            {
                                <h4 class="card-title"> Update Tax Group Master</h4>
                            }
                            else
                            {
                                <h4 class="card-title">Create Tax Group Master</h4>
                            }
                        </div><!--end col-->
                    </div><!--end row-->
                </div>       
                    <div class="card-body">
                        @using (Html.BeginForm(actionName, controllerName, FormMethod.Post))
                        {
                            @Html.HiddenFor(m => m.GeneralTaxGroupMasterId)
                            <div class="row">
                                <div class="col-lg-4">
                                    <div class="mb-3">
                                        @Html.LabelFor(x => x.TaxGroupName, new { @class = "form-label required" })
                                        @Html.TextBoxFor(x => x.TaxGroupName, new { @class = "form-control" })
                                        @Html.ValidationMessageFor(x => x.TaxGroupName)
                                    </div>
                                </div>
                                <div class="col-lg-4">
                                    <div class="mb-3 multiselect-dropdown">
                                        @Html.LabelFor(x => x.GeneralTaxMasterIds, new { @class = "form-label required" })
                                        @Html.ListBoxFor(m => m.GeneralTaxMasterIds, new SelectList(Model.AllTaxList, "GeneralTaxMasterId", "TaxName"), new { @class = "selectpicker form-control" })
                                        @Html.ValidationMessageFor(x => x.GeneralTaxMasterIds)
                                    </div>
                                </div>
                                <div class="col-lg-4">
                                    <div class="mb-3">
                                        @Html.LabelFor(x => x.IsOtherState, new { @class = "form-label" })
                                        <div class="form-check">
                                            @Html.CheckBoxFor(x => x.IsOtherState, new { @class = "form-check-input" })
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-success">@AdminResources.LabelSave</button>
                            <a href="../@controllerName/List" class="btn btn-secondary"><i class="ft-x"></i>@AdminResources.LabelCancel</a>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
